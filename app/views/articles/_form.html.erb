<%= simple_form_for article do |f| %>
  <%= f.input :designation, label: 'design article', error: 'rentrez nom d article' %>
  <%= f.input :prix, label: 'Prix', error: 'le prix ?'%>
 <%= f.button :submit %></button>

 <% end %>
